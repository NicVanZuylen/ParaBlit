# ParaBlit

ParaBlit is my second attempt at making a sophisticated industry-standard render that might one day power the graphics of finished games.

The name 'ParaBlit' is derrived from the (possibly flawed) concept that GPUs are simply blitting 1x1 images in parallel when writing to render targets.
